---
title: Networks
description: Chain IDs, endpoints, and the currently deployed allora-chain version for each Allora network.
persona: Builder or operator
verified_against: live abci_info and cosmos/upgrade applied_plan on both networks, 2026-08-19
last_reviewed: 2026-08-19
---

import { Callout, Code } from 'nextra/components'
import { CHAIN_VERSION_MAINNET, CHAIN_VERSION_TESTNET } from '../../components/versions'
import NetworksTable, { NetworkDetails } from '../../components/NetworksTable'
import { NetworkValue } from '../../components/NetworkValue'

# Networks

> Chain IDs, endpoints, and the currently deployed `allora-chain` version for each Allora network.

The Allora Network runs as two public networks: a **testnet** for development and integration, and
the production **mainnet**. Each may run a different `allora-chain` version, which also determines the
`emissions` REST/gRPC API version: testnet runs {CHAIN_VERSION_TESTNET} and serves
<Code><NetworkValue network="testnet" field="emissions_namespace"/></Code>, mainnet runs {CHAIN_VERSION_MAINNET} and
serves <Code><NetworkValue network="mainnet" field="emissions_namespace"/></Code>.

<NetworksTable />

The tables on this page are rendered from a machine-readable manifest served at `/api/networks.json`.
Agents and scripts can read the same chain IDs, endpoints, and versions from there instead of scraping
this page.

<Callout type="warning">
Deployed versions change with each [software upgrade](/operate/validators/software-upgrades). Testnet is
typically upgraded ahead of mainnet, so features from a newer release (such as the v0.17.0 multi-label
and labeled network-inference APIs) may be available on testnet before they reach mainnet. Always
confirm against the [allora-chain releases](https://github.com/allora-network/allora-chain/releases)
and the [Release Notes](/reference/release-notes).
</Callout>

## Testnet

<NetworkDetails network="testnet" />

Use the testnet for building and testing integrations, running workers/reputers, and trying features
before they ship to mainnet. For wallet creation and faucet funding, see
[Setup Wallet](/get-started/setup-wallet).

## Mainnet

<NetworkDetails network="mainnet" />

Mainnet has no faucet — fund addresses with ALLO yourself.

<Callout type="info">
The `emissions` API version segment is per-network — always pick the one matching the network you are
querying: <Code><NetworkValue network="testnet" field="emissions_namespace"/></Code> on testnet and
<Code><NetworkValue network="mainnet" field="emissions_namespace"/></Code> on mainnet. Since v0.17.0 these
network-inference endpoints return [labeled network-inference bundles](/consume/api) rather than a
single unlabeled value.
</Callout>

## Related

- [Allora API Endpoint](/consume/api) — querying inferences over REST
- [RPC JSON Data Access](/consume/rpc-grpc) — querying over RPC JSON
- [Setup Wallet](/get-started/setup-wallet) — RPC JSON URL and Chain ID configuration
- [Software Upgrades](/operate/validators/software-upgrades) — how network versions are upgraded
